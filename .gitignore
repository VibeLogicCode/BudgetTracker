node_modules/
/data/
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.local
coverage/

.tmp-data/
real-statements/
next-env.d.ts
*.tsbuildinfo
/.next/
/out/
/public/scanner/

.vscode/

# Design references (mockups and exported prototypes). Never shipped, never published.
/UI Component/

# Screen recordings dropped in the repo root while demoing a change. Never part of the
# source tree, and this is a public repository -- a recording of a real household ledger is
# exactly the thing that must not be published by an absent-minded `git add -A`.
screen-*.mp4
